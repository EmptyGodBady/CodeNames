export enum ETeamIdentifiers {
  TeamA = "TeamA",
  TeamB = "TeamB",
}
export enum ERootEndpoints {
  User = "/api/user",
  Lobby = "/api/lobby",
}
export enum ECollections {
  Users = "users",
  Lobby = "lobby",
  Messages = "messages",
}
export enum EUserRole {
  Spectator = "spectator",
  Spy = "spy",
  Scout = "scout",
}
