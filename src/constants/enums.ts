export enum ETeamIdentifiers {
  TeamA = "TeamA",
  TeamB = "TeamB",
}
export enum ERootEndpoints {
  User = "/api/user",
}
export enum ECollections {
  Users = "users",
  Messages = "messages",
}
export enum EUserRole {
  Spectator = "spectator",
  Spy = "spy",
  Scout = "scout",
}
